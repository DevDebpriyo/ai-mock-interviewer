/**
 * This module remains for backward compatibility. The LiveKit migration
 * no longer relies on Vapi, so we export a null placeholder to avoid
 * bundling the Vapi SDK.
 */
export const vapi = null;
